{
  "name": "openlink",
  "version": "1.0.0",
  "description": "SDK for integrating Web3 wallet authentication into existing Web2 services on Solana",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces && rm -rf node_modules"
  },
  "keywords": [
    "solana",
    "web3",
    "authentication",
    "wallet",
    "oauth",
    "sdk"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}

